name: Build and publish package

# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the master branch
on:
  push:
    tags:
      - '*.*.*'
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@master

      - name: Set release env
        run: |
          major_version=7 \
          && echo "MAJOR_VERSION=${major_version}" >> $GITHUB_ENV \
          && [[ $major_version = 5 ]] && echo "PROJECT_TARGET=netstandard2.1" >> $GITHUB_ENV || [[ $major_version = 6 ]] && echo "PROJECT_TARGET=net6.0" >> $GITHUB_ENV || [[ $major_version = 8 ]] && echo "PROJECT_TARGET=net8.0" >> $GITHUB_ENV || echo "PROJECT_TARGET=net9.0" >> $GITHUB_ENV \
          && echo "RELEASE_VERSION=9.9.9" >> $GITHUB_ENV

      - name: Setup .NET9 environment
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: '9.x.x'

      - name: Build project
        run: |
          dotnet build ./Laraue.EfCoreTriggers.sln --framework ${{env.PROJECT_TARGET}} --configuration Release -p:Version=${{env.RELEASE_VERSION}}

      - name: Log path to NuGet package
        run: |
          echo "NuGet packages:"
          ls -lh ./out/packages/*.nupkg